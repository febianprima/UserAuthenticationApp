/**
 * @format
 */

import { AppRegistry } from 'react-native';
import AuthenticationApp from './src/AuthenticationApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => AuthenticationApp);
